snippet plugin
  local present, ${2:name} = pcall(require, '${1:plugin}')

  if not present then
    return
  end

  ${2:name}.setup({
    ${3:config...}
  })

snippet vdebug
  print(vim.inspect.inspect(${1}))
